<!DOCTYPE html>
<html lang="zxx">
  <head>
    <meta charset="utf-8" />
    <title>Data-Sitters tl;dr</title>
    <meta name="generator" content="Eleventy">
    <meta name="robots" content="index,follow,max-image-preview:large" />

    <!-- mobile responsive meta -->
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, maximum-scale=1"
    />

    {% set siteBaseUrl = "https://datasittersclub.github.io" %}
    {% set canonicalUrl = siteBaseUrl + (page.url | url) %}

    {% if featureImg1 %}
      {% set previewImageUrl = featureImg1 %}
      {% if featureImg1[0] == "/" %}
        {% set previewImageUrl = siteBaseUrl + featureImg1 %}
      {% endif %}
    {% endif %}

    <meta property="og:title" content="{{ title | default('Data-Sitters tl;dr') }}" />
    <meta property="og:description" content="{{ subTitle | default('Just enough digital humanities to get started.') }}" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="{{ canonicalUrl }}" />
    <link rel="canonical" href="{{ canonicalUrl }}" />
    <meta name="twitter:card" content="{{ featureImg1 and 'summary_large_image' or 'summary' }}" />
    <meta name="twitter:title" content="{{ title | default('Data-Sitters tl;dr') }}" />
    <meta name="twitter:description" content="{{ subTitle | default('Just enough digital humanities to get started.') }}" />

    {% if featureImg1 %}
    <meta property="og:image" content="{{ previewImageUrl }}" />
    <meta property="og:image:secure_url" content="{{ previewImageUrl }}" />
    <meta property="og:image:type" content="image/jpeg" />
    <meta name="twitter:image" content="{{ previewImageUrl }}" />
    {% endif %}

    <!-- Slick Carousel -->
    <link rel="stylesheet" href="/tldr/assets/vendor/slick/slick.css" />
    <link rel="stylesheet" href="/tldr/assets/vendor/slick/slick-theme.css" />
    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="/tldr/assets/vendor/font-awesome/css/font-awesome.min.css"
    />
    <!-- Bootstrap -->
    <link rel="stylesheet" href="/tldr/assets/vendor/bootstrap/bootstrap.min.css" />
    <!-- Stylesheets -->
    <link href="/tldr/assets/css/style.css" rel="stylesheet" />

    <!--Favicon-->
    <link rel="shortcut icon" href="/tldr/assets/images/favIcon.png" type="image/x-icon" />
    <link rel="icon" href="/tldr/assets/images/favIcon.png" type="image/x-icon" />
  </head>

  <body{% block bodyAttrs %}{% endblock %}>
  {% block navbar %}
    <nav class="navbar navbar-expand-lg site-navigation">
      <div class="container">
        <a class="navbar-brand" href="https://datasittersclub.github.io/site/">
          <img src="{{nav.navLogo}}" alt="nav-logo" />
        </a>
        <button
          class="navbar-toggler collapsed"
          type="button"
          data-toggle="collapse"
          data-target="#sitenavbar"
        >
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      
        <div class="collapse navbar-collapse" id="sitenavbar">
          <ul class="navbar-nav ml-auto main-nav">
            <li class="nav-item">
                <a class="nav-link" href="/tldr/">Home</a>
            </li>
                        <li class="nav-item">
                <a class="nav-link" href="/tldr/about/">About</a> 
            </li>
                            
            
          </ul>
        </div>
      </div>
    </nav>
  {% endblock %}

  {% block content %}
    {{content | safe}}
  {% endblock %}

  

  
    <!-- <script src="js/vendor.js"></script> -->
    <script src="/tldr/assets/vendor/jQuery/jquery.min.js"></script>
    <script src="/tldr/assets/vendor/bootstrap/bootstrap.min.js"></script>
    <script src="/tldr/assets/js/script.js"></script>
  </body>
</html>

